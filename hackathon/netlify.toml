[build]
  # Tell Netlify where the actual app lives
  base = "hackathon"
  
  # Command to run inside the 'hackathon' folder
  command = "npm run build"
  
  # Where the build output is located relative to the 'hackathon' folder
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"